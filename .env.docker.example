# Example env file for docker-compose (DO NOT commit secrets)
# Copy to .env.docker and edit before running: `docker compose --env-file .env.docker up --build`

# MySQL (docker) - MUST be non-empty for the official image
MYSQL_ROOT_PASSWORD=secret
DB_NAME=sotekre_dev
DB_USER=root
DB_PASS=secret
DB_HOST=db
DB_PORT=3306

# Backend / frontend
PORT=8080
NEXT_PUBLIC_API_URL=http://backend:8080
